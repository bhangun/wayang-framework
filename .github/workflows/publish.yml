name: Publish — GitHub Packages

# Triggers:
#   1. Automatically on every push to `main` (publishes a SNAPSHOT)
#   2. Manually via the GitHub UI, where you can specify a release version
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      release_version:
        description: "Release version to publish (e.g. 1.0.0). Leave empty to publish SNAPSHOT."
        required: false
        type: string

permissions:
  contents: read
  packages: write  # Required to publish to GitHub Packages

jobs:
  publish:
    name: Publish to GitHub Packages
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup GraalVM (Java 25)
        uses: actions/setup-java@v4
        with:
          distribution: graalvm
          java-version: "25"
          cache: maven
          # This auto-creates a ~/.m2/settings.xml with the github server configured
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      # ── Gollek SPI/SDK Bootstrap ────────────────────────────────────────────
      - name: Checkout gollek (SPI/SDK bootstrap)
        uses: actions/checkout@v4
        with:
          repository: bhangun/gollek
          path: _gollek
          ref: main

      - name: Cache Gradle packages (gollek)
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gollek-gradle-${{ hashFiles('_gollek/gradle/wrapper/gradle-wrapper.properties', '_gollek/build.gradle.kts') }}
          restore-keys: |
            gollek-gradle-

      - name: Publish gollek SPI & SDK to Maven Local
        working-directory: _gollek
        env:
          GRADLE_OPTS: "-Xmx2G -Dorg.gradle.daemon=false"
        run: |
          TASKS=(
            :spi:gollek-spi:publishToMavenLocal
            :spi:gollek-spi-audio:publishToMavenLocal
            :spi:gollek-spi-image:publishToMavenLocal
            :spi:gollek-spi-inference:publishToMavenLocal
            :spi:gollek-spi-multimodal:publishToMavenLocal
            :spi:gollek-spi-plugin:publishToMavenLocal
            :spi:gollek-spi-runtime:publishToMavenLocal
            :sdk:gollek-sdk-core:publishToMavenLocal
            :sdk:gollek-sdk:publishToMavenLocal
            :sdk:gollek-sdk-local:publishToMavenLocal
          )
          ./gradlew "${TASKS[@]}" --no-daemon -x test --parallel
      # ────────────────────────────────────────────────────────────────────────

      # ── Optional: Set a release version if triggered manually ──────────────
      - name: Set Release Version
        if: ${{ inputs.release_version != '' }}
        run: |
          echo "Setting release version to ${{ inputs.release_version }}"
          ./mvnw versions:set -DnewVersion=${{ inputs.release_version }} -DgenerateBackupPoms=false -q

      # ── Build all modules (skip tests for speed; CI already tested) ────────
      - name: Build all modules
        run: ./mvnw clean package -DskipTests -Pci -q

      # ── Deploy to GitHub Packages ───────────────────────────────────────────
      - name: Deploy to GitHub Packages
        run: |
          ./mvnw deploy \
            -DskipTests \
            -Pci \
            -q
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ── Tag a release commit if a specific version was provided ───────────
      - name: Tag Release
        if: ${{ inputs.release_version != '' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -am "chore: release ${{ inputs.release_version }} [skip ci]" || true
          git tag "v${{ inputs.release_version }}"
          git push origin "v${{ inputs.release_version }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
