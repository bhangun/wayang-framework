# Wayang Memory

The `wayang-memory` module provides memory management contracts and
implementations for agent systems. It covers memory entries, scopes,
hierarchies, policies, snapshots, retrieval, and visualization.

Choose a memory scope and policy for each agent, then connect a store and
retriever appropriate to the deployment and retention requirements.
