# Wayang Workflow

The `wayang-workflow` module models declarative workflows, steps, transitions,
conditions, and workflow execution.

Define a workflow as typed steps and transitions, then provide a workflow
engine to execute it with the context, tools, and policies supplied by the
runtime.
