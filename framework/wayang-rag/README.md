# Wayang RAG

The `wayang-rag` module defines retrieval-augmented generation contracts for
chunking, embedding, vector storage, retrieval strategies, and RAG pipelines.

Compose a chunker, embedding provider, vector store, and retrieval strategy,
then pass the retrieved context to the model provider used by the agent.
