# Wayang MCP

The `wayang-mcp` module integrates Model Context Protocol servers with Wayang.
It supports HTTP and STDIO transports, server configuration loading, JSON-RPC
tool calls, tool descriptors, and skill bridges.

Configure one or more MCP servers, select the transport, and expose the
resulting tools through the MCP client or skill adapter.
