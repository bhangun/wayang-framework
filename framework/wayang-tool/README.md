# Wayang Tool

The `wayang-tool` module defines the tool system for agents. It provides tool
descriptors, invocations, contexts, results, registries, and execution
contracts.

Implement `Tool`, publish a schema-backed descriptor, and register it with the
tool registry so agents can discover and invoke it safely.
