# Wayang Harness

The `wayang-harness` module provides the governed runtime boundary for agent
turns. It covers lifecycle, context, memory, workspace, artifacts, resources,
tools, model execution, approvals, policy chains, checkpoints, and action
journaling.

Use `Harness` and its request, result, and context types as the integration
boundary for running agents with consistent governance and observability.
