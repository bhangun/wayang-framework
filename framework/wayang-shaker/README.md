# Wayang Shaker

The `wayang-shaker` module analyzes and packages agent workflows. It supports
tree-shaking, dependency discovery, workflow analysis, and producing a smaller
agent distribution.

Run the shaker against an agent or workflow definition after all required
extensions are declared so unused resources can be removed safely.
