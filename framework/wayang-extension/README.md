# Wayang Extension

The `wayang-extension` module provides the universal resource and extension
model used across the framework. It defines identifiers, versions, metadata,
states, permissions, principals, capabilities, descriptors, artifacts, and
resource builders.

Use these types at module and plugin boundaries so resources can be described,
versioned, referenced, and represented consistently across runtimes.
