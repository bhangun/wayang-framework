# Wayang SPI

The `wayang-spi` module contains service-provider interfaces for agents,
skills, tools, memory, sandboxes, plugins, and capabilities.

Implement these contracts to extend a Wayang runtime without depending on
concrete framework implementations, and register providers through the
runtime's extension mechanism.
