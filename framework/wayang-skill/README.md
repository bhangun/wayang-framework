# Wayang Skill

The `wayang-skill` module defines reusable skill metadata and execution
contracts for agents. It lets runtimes discover, describe, validate, and invoke
skills independently of a particular agent strategy.

Publish skills with stable descriptors and connect their execution through the
skill provider or registry used by the host runtime.
