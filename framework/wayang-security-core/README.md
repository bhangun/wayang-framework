# Wayang Security Core

The `wayang-security-core` module supplies default security implementations for
policy evaluation, delegation, policy enforcement, and audit integration.

Configure the policy engine and enforcement point in the runtime, and keep
application code dependent on the contracts from `wayang-security-api`.
