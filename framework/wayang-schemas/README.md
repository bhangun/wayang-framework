# Wayang Schemas

The `wayang-schemas` module provides JSON value, schema-building, and registry
utilities for Wayang contracts and extension descriptors.

Use the JSON model and schema registry to publish machine-readable contracts
and validate configuration or protocol payloads consistently.
