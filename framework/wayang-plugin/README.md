# Wayang Plugin

The `wayang-plugin` module provides the plugin system for Wayang extensions.
It covers manifests, dependency resolution, lifecycle, plugin resources, and
capability registration.

Package an extension with a manifest, resolve its dependencies through the
plugin manager, and register capabilities only during the managed lifecycle.
