# Wayang Registry

The `wayang-registry` module provides standard registry resources and drift
detection for published Wayang ecosystem specifications, including A2A, A2UI,
and agentic-commerce entries.

Use the registry and schema types to validate standard resources and detect
changes between the expected and observed registry documents.
