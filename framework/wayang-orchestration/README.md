# Wayang Orchestration

The `wayang-orchestration` module coordinates agent execution across plans,
tasks, providers, tools, and workflow boundaries. It contains orchestration
contracts and default coordination support for multi-step agent work.

Use the orchestration API when a request spans multiple agent operations or
requires centralized lifecycle and result handling.
