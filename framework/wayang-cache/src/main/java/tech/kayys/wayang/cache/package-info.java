/**
 * Caching abstractions for derived Wayang content.
 *
 * <p>Applications can combine cache keys, policies, stores, loaders, and
 * invalidators to reuse safe derived results without coupling the framework to
 * a particular cache implementation.</p>
 *
 * @since 0.0.1
 */
package tech.kayys.wayang.cache;
